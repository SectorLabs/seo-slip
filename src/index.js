const checkers = require('./checkers');
const ci = require('./ci');
const crawler = require('./crawler');
const html = require('./html');

module.exports = {
    checkers,
    ci,
    crawler,
    html,
};
