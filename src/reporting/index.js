const {
    newEmptyAnalysis,
    newEmptyAnalyses,
    mergeItemAnalysis,
    pushItemAnalysis,

    newMessage,
    newEmptyItemReport,
    newEmptyReport,
    mergeItemReport,
    pushItemReport,
    printResults,

    newEmptyItemResult,
    newEmptyResults,
    mergeItemResult,
    pushItemResult,
} = require('./reporting');

module.exports = {
    newEmptyAnalysis,
    newEmptyAnalyses,
    mergeItemAnalysis,
    pushItemAnalysis,

    newMessage,
    newEmptyItemReport,
    newEmptyReport,
    mergeItemReport,
    pushItemReport,
    printResults,

    newEmptyItemResult,
    newEmptyResults,
    mergeItemResult,
    pushItemResult,
};
